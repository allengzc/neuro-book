# Current Playthrough

本文件记录当前 RP 进程的可检查摘要。它不是 canonical lorebook，也不是 actor 长期记忆。

## 当前场景

### 起始场景

位置暂用 `config.yaml` 的 `fallbackScene.location`。GM 初始化后，把用户可见的当前场景摘要写在这里。

## 当前 Tick

### 000001

示例 Tick 位于 `ticks/000001/`，用于说明 runs 目录如何保存过程产物。正式运行时可以覆盖或删除示例内容。
